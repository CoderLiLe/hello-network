# 网络实用命令与故障排查

> 综合自小码哥教育课程和极客时间"趣谈网络协议"专栏

## 基础排查工具

| 命令 | 用途 | 示例 |
|------|------|------|
| `ping` | 测试连通性与延迟 | `ping -c 4 baidu.com` |
| `tracert` / `traceroute` | 追踪路由路径 | `tracert baidu.com` |
| `ifconfig` / `ipconfig` | 查看网络接口配置 | `ifconfig eth0` |
| `netstat` | 查看端口与连接状态 | `netstat -anp \| grep 80` |
| `nslookup` / `dig` | DNS 查询 | `nslookup baidu.com` |
| `arp` | 查看/管理 ARP 缓存 | `arp -a` |
| `telnet` | 测试端口连通性 | `telnet 192.168.1.1 80` |
| `curl` | HTTP 请求测试 | `curl -v http://example.com` |
| `tcpdump` | 抓包分析 | `tcpdump -i eth0 port 80` |
| `ss` | 查看 socket 统计 | `ss -tuln` |

## 各层排查方法

### 应用层排查

**HTTP 状态码速查：**

| 范围 | 含义 | 常见问题 |
|------|------|----------|
| 1xx | 信息 | 100 Continue |
| 2xx | 成功 | 200 OK |
| 3xx | 重定向 | 301/302 需要检查 Location |
| 4xx | 客户端错误 | 400(参数错误) 401(未认证) 403(无权限) 404(未找到) |
| 5xx | 服务器错误 | 500(内部错误) 502(网关错误) 503(过载) 504(超时) |

**常用 curl 调试：**
```bash
# 查看完整 HTTP 响应头
curl -I https://example.com

# 查看完整请求响应
curl -v https://example.com

# 指定请求方法
curl -X POST -H "Content-Type: application/json" \
  -d '{"key":"value"}' https://api.example.com/endpoint

# 跟随重定向
curl -L http://example.com
```

### DNS 排查

```bash
# 查询域名解析
nslookup example.com
dig example.com

# 指定 DNS 服务器
nslookup example.com 8.8.8.8

# 反向解析
nslookup 93.184.216.34

# 查看 DNS 缓存（Windows）
ipconfig /displaydns
ipconfig /flushdns
```

**常见 DNS 服务器：**

| DNS 服务商 | IPv4 | IPv6 |
|-----------|------|------|
| 114DNS | 114.114.114.114 | 114.114.115.115 |
| 阿里 DNS | 223.5.5.5 | 2400:3200::1 |
| 腾讯 DNS | 119.29.29.29 | - |
| Google DNS | 8.8.8.8 | 2001:4860:4860::8888 |
| Cloudflare | 1.1.1.1 | 2606:4700:4700::1111 |

### TCP/UDP 排查

```bash
# 查看端口监听状态
netstat -tuln
ss -tuln

# 查看 TCP 连接状态
netstat -an | grep ESTABLISHED

# 测试 TCP 端口是否开放
telnet host port
nc -zv host port

# 查看路由追踪（判断网络路径）
tracert baidu.com  # Windows
traceroute baidu.com  # Mac/Linux

# 查看 MTU 路径
ping -M do -s 1472 baidu.com  # Linux
```

**TCP 连接状态解释：**

| 状态 | 含义 |
|------|------|
| LISTEN | 服务端正在监听端口 |
| SYN-SENT | 客户端发送了 SYN，等待确认 |
| SYN-RCVD | 服务端收到 SYN，发送了 SYN+ACK |
| ESTABLISHED | 连接已建立，正常通信 |
| FIN-WAIT-1/2 | 主动关闭方等待对方确认 |
| TIME-WAIT | 主动关闭方等待 2MSL |
| CLOSE-WAIT | 被动关闭方等待应用关闭 |
| LAST-ACK | 被动关闭方等待最后一个 ACK |

### 网络层排查

```bash
# 查看路由表
netstat -rn
route -n

# ping 测试（带参数）
ping -c 4 -s 1472 -M do baidu.com  # MTU 测试
ping -c 4 -t 5 baidu.com  # 设置 TTL

# 查看 ARP 表
arp -a

# 查看接口 IP
ifconfig
ip addr  # Linux

# 查看 DHCP 信息
ipconfig /all  # Windows
```

### Ping 返回的 TTL 判断

| TTL 值 | 推测操作系统 |
|--------|-------------|
| 128 | Windows |
| 64 | Linux / macOS |
| 255 | Unix / 网络设备 |

通过 TTL 可以估算经过的路由器数量：`初始TTL - 返回TTL = 跳数`

## 常见问题排查流程

### 问题 1：网页打不开

```
1. ping 域名 → 通？ → DNS 解析正常
2. ping IP → 通？ → 网络层正常
3. telnet IP 80 → 通？ → TCP 层正常
4. curl -v http://IP → 有响应？ → HTTP 层正常
```

### 问题 2：虚拟机无法上网

```
1. ifconfig → IP 地址是否为 169.254.x.x ？ → DHCP 未获取到
2. ping 网关 → 通？ → 网关配置正确
3. ping 8.8.8.8 → 通？ → DNS 问题
4. nslookup baidu.com → 有结果？ → DNS 配置问题
```

### 问题 3：网络慢

```
1. ping 网关 → 延迟？ → 局域网瓶颈
2. ping 外网 → 延迟？ → 互联网瓶颈
3. tracert 目标 → 哪一跳延迟高？ → 定位问题节点
4. 检查带宽占用 → netstat -s → 丢包率
5. 检查 TCP 重传率 → 拥塞控制是否生效
```

### 问题 4：端口不通

```bash
# 本地检查端口是否监听
netstat -an | grep 8080

# 防火墙是否放行
iptables -L -n  # Linux

# 远程测试端口
telnet server-ip 8080

# 使用 nc 测试
nc -zv server-ip 8080
```

## Wireshark 抓包技巧

### 常用过滤表达式

| 表达式 | 用途 |
|--------|------|
| `ip.addr == 192.168.1.1` | 过滤特定 IP |
| `tcp.port == 80` | 过滤特定端口 |
| `http` | 只显示 HTTP 包 |
| `tcp.flags.syn == 1` | 显示 SYN 包（握手） |
| `tcp.analysis.lost_segment` | 显示丢失的段 |
| `tcp.analysis.retransmission` | 显示重传包 |
| `dns` | 显示 DNS 查询与响应 |
| `tls.handshake.type == 1` | TLS Client Hello |

### HTTPS 解密
1. 设置环境变量 `SSLKEYLOGFILE=~/sslkeys.log`
2. 重启浏览器
3. Wireshark 设置：编辑 → 首选项 → Protocols → TLS → 选择 key 文件

## 网络实验环境搭建

### 最小实验环境

```bash
# 1. 查看本机网络
ifconfig
netstat -rn

# 2. 启动一个简单的 HTTP 服务器
python3 -m http.server 8000

# 3. 用 curl 测试
curl -v http://localhost:8000

# 4. 用 tcpdump 抓包
sudo tcpdump -i lo0 -X port 8000
```

### 多容器网络实验

```bash
# 创建两个容器
docker run -d --name server nginx
docker run -it --name client alpine sh

# 查看容器网络
docker network ls
docker inspect bridge

# 容器间通信测试
docker exec client ping server
```

---

> 参考课程：小码哥教育（李明杰）+ 极客时间"趣谈网络协议"（刘超）
