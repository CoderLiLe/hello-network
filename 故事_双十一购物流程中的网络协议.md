# 双十一购物流程中的网络协议

> 基于极客时间"趣谈网络协议"专栏（刘超）整理
> 完整展示了从打开 App 到下单成功的全过程，涉及 40+ 网络协议

## 阶段总览

```
1. 部署电商平台 → 2. BGP路由广播 → 3. DNS域名解析
     → 4. CDN静态资源 → 5. 建立HTTPS连接 → 6. 封装网络包
     → 7. 路由转发/重传控制 → 8. NAT转换 → 9. VXLAN隧道/RPC调用
     → 10. 数据入库/返回成功
```

---

## 第一阶段：部署电商平台 (基础架构)

涉及协议：**VXLAN、BGP、IPsec VPN、DNS**

- 在公有云上创建 **VPC（虚拟私有网络）**，分配 IP 段
- 多可用区部署，每个可用区分配 Subnet
- **VXLAN** 隔离不同用户的 VPC（分配 VXLAN ID）
- 数据库、缓存、应用服务分层部署
- 负载均衡（SLB）配置公网 IP
- **VPN** / **IPsec** 连接不同 Region 的 VPC
- **CDN** 下发静态资源到边缘节点

**涉及文档：** 云中网络、数据中心、VPN、GRE/VXLAN

---

## 第二阶段：广播路由 (BGP)

涉及协议：**BGP（eBGP/iBGP）**

- 边界路由器通过 **eBGP** 将数据中心的外网 IP 广播到全网
- 内部通过 **iBGP** 同步路由信息
- 每个运营商的路由器都知道了如何到达目标 IP

**类比：** 告诉全世界"我这里可以买东西，地址在这里"

**涉及文档：** 路由协议

---

## 第三阶段：域名解析 (DNS/HTTPDNS)

涉及协议：**DNS、HTTPDNS、GSLB**

```
用户打开 App
    → 查询 DNS 缓存
    → 本地 DNS 递归查询（根域名 → .com → 权威DNS）
    → 智能 DNS / GSLB 根据地理位置返回最近的 SLB IP
```

- 传统 **DNS**：递归查询，从根域名到权威 DNS
- **HTTPDNS**：直接通过 HTTP 接口获取 IP，避免 DNS 劫持
- **GSLB（全局负载均衡）**：根据用户位置和运营商，返回最近的 IP

**涉及文档：** DNS协议、HTTPDNS

---

## 第四阶段：加载静态资源 (CDN)

涉及协议：**HTTP、CDN**

```
用户浏览商品图片
    → DNS 解析静态资源域名
    → CNAME 指向 CDN GSLB
    → GSLB 选择最近边缘节点
    → 边缘节点命中 → 直接返回
    → 边缘节点未命中 → 逐级回源
```

**涉及文档：** CDN、HTTP缓存

---

## 第五阶段：建立安全连接 (TCP + TLS)

涉及协议：**TCP、TLS/SSL、HTTPS**

### TCP 三次握手
```
CLOSED → SYN-SENT → ESTABLISHED (客户端)
LISTEN → SYN-RCVD → ESTABLISHED (服务器)
```

### TLS 握手
```
① Client Hello（版本、加密套件、随机数）
② Server Hello（选择加密套件、随机数）
③ Certificate（服务器证书）
④ Server Key Exchange（ECDHE 参数）
⑤ Server Hello Done
⑥ Client Key Exchange（Pre-master，用公钥加密）
⑦ Change Cipher Spec（切换对称加密）
⑧ Finished（验证握手完整性）
⑨-⑩ 服务器同样操作
```

> TCP 三次握手 + TLS 握手的耗时是 HTTPS 的主要延迟来源。
> HTTP/3 (QUIC) 通过一次 RTT 完成连接建立和加密。

**涉及文档：** TCP协议、HTTPS

---

## 第六阶段：封装网络包 (HTTP → TCP → IP → MAC)

涉及协议：**HTTP、TCP、IP、ARP、DHCP**

### 各层封装过程

用户点击"下单"，应用层构造 HTTP 请求：

```
POST /purchaseOrder HTTP/1.1
Host: www.example.com
Content-Type: application/json; charset=utf-8
Content-Length: nnn

{"order": {"date": "...", "product": "趣谈网络协议", "price": "68"}}
```

**传输层（TCP）：** 切分成 Segment，加上源端口(随机)和目标端口(443)

**网络层（IP）：** 加上源 IP（手机）和目标 IP（SLB公网IP），判断不在同一网段，走默认网关

**数据链路层（MAC）：** 通过 **ARP** 获取网关 MAC 地址

**完整网络包格式：**
```
| MAC头(源MAC + 目标MAC) | IP头(源IP + 目标IP) | TCP头(端口) | HTTP数据 |
```

**涉及文档：** HTTP协议、TCP协议、IP协议、MAC地址

---

## 第七阶段：网络传输 (移动网络 + 互联网路由)

涉及协议：**GTP、NAT、OSPF、BGP、TCP（重传/拥塞控制）**

### 移动网络路径
```
手机 → eNodeB(基站) → SGW → PGW → 互联网
```

手机到 PGW 之间有 **GTP 隧道**，双层封装：
```
外层: eNodeB-IP → SGW-IP → PGW-IP
内层: 手机-IP → SLB-公网-IP
```

### NAT 转换
在 PGW 出口，**NAT** 将手机私网 IP 转换为公网 IP。

### 互联网路由
```
自治系统(AS) 之间通过 eBGP 交换路由
每个 AS 内部通过 iBGP / OSPF / 静态路由 转发
一跳一跳地更换 MAC 地址，IP 地址不变
```

### TCP 可靠性机制
- 滑动窗口协议控制发送速率
- 超时重传（收不到 ACK 就重发）
- 拥塞控制（慢开始、拥塞避免、快重传、快恢复）
- **SACK** 选择性确认，只重传丢失的包

**涉及文档：** 移动网络、NAT、路由协议、TCP协议、ICMP

---

## 第八阶段：数据中心入口 (NAT → SLB)

涉及协议：**NAT、VXLAN**

```
SLB 公网 IP → NAT 转换为私网 IP → VXLAN 隧道 → 后端服务器
```

1. 包到达 SLB 的公网网口
2. 虚拟网关做 **NAT**：公网 IP → VPC 私网 IP
3. 包进入 **VXLAN** 隧道（标记 VXLAN ID 隔离租户）
4. 物理交换机根据外层 IP 转发到目标物理机
5. OVS 解封装，交给目标虚拟机

**涉及文档：** 云中网络、GRE/VXLAN

---

## 第九阶段：RPC 调用 (服务间通信)

涉及协议：**HTTP/2、Dubbo、Hessian2、gRPC、Netty**

```
SLB(四层负载均衡) → HAProxy → Controller层 → 组合服务层 → 基础服务层
                          ↓                        ↓
                     RPC 调用(Dubbo)           RPC 调用(Dubbo)
                     Hessian2 序列化           Hessian2 序列化
                     Netty 传输                Netty 传输
```

- **四层负载均衡**：只解析到 TCP 层，转发给 Controller
- **RPC 框架**（Dubbo）：从注册中心获取服务列表
- **序列化**（Hessian2/Protobuf）：将调用参数二进制化
- **Netty**：基于事件驱动的异步网络框架（IO 多路复用）
- **幂等设计**：使用 CAS（乐观锁）防止重复扣减

**涉及文档：** RPC协议、SOAP、RESTful、二进制RPC、跨语言RPC

---

## 第十阶段：数据入库 (最终完成)

涉及协议：**分布式事务、CAS、2PC**

1. 调用库存服务（CAS 扣减库存）
2. 调用优惠券服务（核销优惠券）
3. 调用订单服务（生成订单）
4. 数据写入分布式数据库
5. 返回"下单成功"响应

响应包沿原路径返回，**层层解封装**：
```
MAC解 → IP解 → TCP解 → TLS解密 → HTTP响应 → 显示"支付成功"
```

---

## 完整协议图谱

```
应用层:    HTTP/HTTPS  DNS/HTTPDNS  CDN  RPC(Dubbo/gRPC)
传输层:    TCP（三次握手/滑动窗口/拥塞控制）  UDP
网络层:    IP（路由/分片）  ICMP(ping)  ARP  OSPF  BGP
数据链路层: MAC  VLAN(802.1Q)  PPP
物理层:    以太网  光纤  无线
云网络层:  VXLAN  GRE  SDN  OpenFlow  OVS
容器网络层: CNI  Flannel(VXLAN)  Calico(BGP)
```

---

## 学习要点

| 阶段 | 关键问题 | 答案 |
|------|----------|------|
| DNS | 为什么需要 HTTPDNS？ | 避免传统 DNS 劫持和跨网访问问题 |
| TCP | 为什么三次握手不是两次？ | 防止失效的连接请求到达服务器 |
| TLS | 非对称加密 vs 对称加密？ | 非对称用于密钥交换，对称用于实际数据传输 |
| 路由 | 为什么需要 MAC + IP？ | IP 用于跨网络寻址，MAC 用于局域网内通信 |
| 云计算 | 什么是 VXLAN？ | 在三层网络上构建二层 Overlay 网络，隔离租户 |
| 容器 | Flannel vs Calico？ | Flannel 用 Overlay，Calico 用纯三层 + BGP |
| RPC | 为什么需要序列化？ | 将内存中的对象转为二进制流以便网络传输 |

---
