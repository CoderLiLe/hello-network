# 第29讲 容器网络

容器 = 集装箱：**打包** + **标准**，比虚拟机更轻量的隔离。

## namespace（网络隔离）

Linux namespace 让进程看到独立网络栈，由 `ip netns` 管理：

```bash
ip netns add routerns
ip netns exec routerns sysctl -w net.ipv4.ip_forward=1
ip link set taprouter netns routerns
ip netns exec routerns ip addr add 192.168.1.1/24 dev taprouter
# NAT 规则
ip netns exec routerns iptables -t nat -A PREROUTING -d X.X.X.X -j DNAT --to 192.168.1.3
ip netns exec routerns iptables -t nat -A POSTROUTING -s 192.168.1.3 -j SNAT --to X.X.X.X
```

## cgroup（资源限制）

限制 CPU、内存、块设备 IO。`net_cls` 用 classid 标记网络包，配合 TC 做流量控制：

```bash
mkdir /sys/fs/cgroup/net_cls/a
echo 0x00010010 > /sys/fs/cgroup/net_cls/a/net_cls.classid
tc filter add dev eth0 protocol ip parent 1:0 prio 1 handle 1: cgroup
```

## Docker 网络

### veth pair

替代虚拟机的 TUN/TAP，一端连 docker0 网桥，一端塞入容器 namespace：

```bash
ip link add veth1 type veth peer name veth2
ip link set veth1 master docker0 && ip link set veth1 up
docker inspect '--format={{.State.Pid}}' test  # 获 pid
ln -s /proc/<pid>/ns/net /var/run/netns/<pid>
ip link set veth2 netns <pid>
ip netns exec <pid> ip addr add 172.17.0.2/24 dev eth0
```

### NAT 出网

`-A POSTROUTING -s 172.17.0.0/16 ! -o docker0 -j MASQUERADE`，conntrack 记录映射。

### 端口映射

- **docker-proxy**：用户态监听宿主机端口转发
- **DNAT**：`-A DOCKER -p tcp --dport 10080 -j DNAT --to 172.17.0.2:80`
